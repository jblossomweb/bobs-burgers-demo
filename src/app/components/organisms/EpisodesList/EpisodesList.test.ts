import storySnaps from 'test/utils/storySnaps'
import * as stories from './EpisodesList.stories'

describe('app/components/organisms/EpisodesList', () => {
  describe('stories', () => {
    storySnaps(stories)
  })
})
