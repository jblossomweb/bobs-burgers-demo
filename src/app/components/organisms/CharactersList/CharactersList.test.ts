import storySnaps from 'test/utils/storySnaps'
import * as stories from './CharactersList.stories'

describe('app/components/organisms/CharactersList', () => {
  describe('stories', () => {
    storySnaps(stories)
  })
})
