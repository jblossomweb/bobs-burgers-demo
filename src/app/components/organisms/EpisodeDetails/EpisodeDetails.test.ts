import storySnaps from 'test/utils/storySnaps'
import * as stories from './EpisodeDetails.stories'

describe('app/components/organisms/EpisodeDetails', () => {
  describe('stories', () => {
    storySnaps(stories)
  })
})
