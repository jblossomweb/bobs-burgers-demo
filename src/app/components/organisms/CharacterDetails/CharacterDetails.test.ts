import storySnaps from 'test/utils/storySnaps'
import * as stories from './CharacterDetails.stories'

describe('app/components/organisms/CharacterDetails', () => {
  describe('stories', () => {
    storySnaps(stories)
  })
})
