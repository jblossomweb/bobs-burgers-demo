import storySnaps from 'test/utils/storySnaps'
import * as stories from './ContentHeader.stories'

describe('app/components/molecules/ContentHeader', () => {
  describe('stories', () => {
    storySnaps(stories)
  })
})
