import storySnaps from 'test/utils/storySnaps'
import * as stories from './EpisodeCard.stories'

describe('app/components/molecules/EpisodeCard', () => {
  describe('stories', () => {
    storySnaps(stories)
  })
})
