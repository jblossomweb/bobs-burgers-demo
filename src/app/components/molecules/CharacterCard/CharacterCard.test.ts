import storySnaps from 'test/utils/storySnaps'
import * as stories from './CharacterCard.stories'

describe('app/components/molecules/CharacterCard', () => {
  describe('stories', () => {
    storySnaps(stories)
  })
})
