import storySnaps from 'test/utils/storySnaps'
import * as stories from './Avatar.stories'

describe('app/components/atoms/Avatar', () => {
  describe('stories', () => {
    storySnaps(stories)
  })
})
