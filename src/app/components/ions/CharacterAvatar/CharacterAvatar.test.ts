import storySnaps from 'test/utils/storySnaps'
import * as stories from './CharacterAvatar.stories'

describe('app/components/ions/CharacterAvatar', () => {
  describe('stories', () => {
    storySnaps(stories)
  })
})
