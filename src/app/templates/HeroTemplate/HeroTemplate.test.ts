import storySnaps from 'test/utils/storySnaps'
import * as stories from './HeroTemplate.stories'

describe('app/templates/HeroTemplate', () => {
  describe('stories', () => {
    storySnaps(stories)
  })
})
