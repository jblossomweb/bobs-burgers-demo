import storySnaps from 'test/utils/storySnaps'
import * as stories from './ContentTemplate.stories'

describe('app/templates/ContentTemplate', () => {
  describe('stories', () => {
    storySnaps(stories)
  })
})
